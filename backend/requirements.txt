#!/usr/bin/env pip

# Backend dependencies for exampleOne Orchestrator Studio
# This file simply reuses the main orchestrator requirements so you can run:
#   pip install -r backend/requirements.txt
# from the repository root or CI without duplication.

-r orchestrator/requirements.txt
